services:
  - type: web
    name: aiml-healthcare
    env: python
    region: oregon
    plan: free
    branch: main
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn web_app:app"
    envVars:
      - key: PYTHON_VERSION
        value: "3.9"
